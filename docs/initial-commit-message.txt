feat: package gpu_ext patches for NVIDIA 615.71.09

Prepare a source-only patch collection against NVIDIA commit
61dcc93722ecb418bb5f2e00923f05b4b8051dd1. Split the driver changes into
validated UVM memory policies, GPU scheduling hooks and BTF prerequisites.
Use the official upstream RM layout instead of the local installer tree.

Include the application policy, six scheduling modes, process discovery,
systemd integration, source attribution and per-file licensing. Exclude
compiled artifacts, generated BTF headers and the complete NVIDIA source.
Document the first-context polling race and the absence of runtime claims.

Validation:
- Passed sequential git apply --check --index --whitespace=error-all.
- Applied and reverse-restored all three patches on the exact baseline.
- Verified dirty-tree and wrong-base rejection without target changes.
- Passed ShellCheck, launcher fixtures and staged whitespace checks.
- No driver or userspace build, installation, BPF attach or benchmark run.

Assisted-by: Codex:gpt-6
